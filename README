This is a place to keep csv files generated from the targets script for California state wildfire data, taken from: https://www.fire.ca.gov/incidents/

This uses targets, a make-like tool in R

Data folder has csv files, 
Targets folder has R code
